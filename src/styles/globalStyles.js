import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    
    layout: {
        flex: 1,
        backgroundColor: '#000',
        paddingHorizontal: 10,
    }
});